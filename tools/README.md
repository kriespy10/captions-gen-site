# Blog generator

Edit `tools/bodies.py` / `tools/bodies2.py` (post text), then from the repo root run:

    python tools/genblog.py

Regenerates `blog/*.html`, `blog/index.html` and `sitemap.xml`. Commit and push.
